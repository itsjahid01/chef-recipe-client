import React from "react";
import { Outlet } from "react-router-dom";

const Root = () => {
  return (
    <div>
      <h2>fhdjsbj</h2>
      <Outlet></Outlet>
    </div>
  );
};

export default Root;
